- name: variables
  hosts: local
  connection: local
  become: yes
  vars:
    # name: vinay
    course: bca
    # roll: 31

  tasks:
  - name: student information
    ansible.builtin.debug:
      msg: "This is {{name}} studying {{course}} with roll no {{roll}}"

